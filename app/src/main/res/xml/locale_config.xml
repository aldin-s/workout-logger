<?xml version="1.0" encoding="utf-8"?>
<!--
    Per-App Language configuration for Android 13+.
    This enables the language selector in Android System Settings > Apps > REPS > Language.
    
    AndroidX AppCompat handles backwards compatibility for older Android versions.
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="de" />
    <locale android:name="en" />
</locale-config>
